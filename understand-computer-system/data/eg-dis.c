/* Demonstration of two's complement numbers in disassembly */

int proc(int *a, int x, int y)
{
    int b[90];
    int t[4];

    b[2] = a[4] + t[3];
    a[7] = b[30];
    b[57] = x + y;
    return a[6];
}

int main()
{
    int a[10];
    return proc(a, 5, 7);
}
